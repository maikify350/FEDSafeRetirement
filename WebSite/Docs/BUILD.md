# Website Build and Preview

Run commands from `WebSite/`.

## Canonical Commands

```bash
npm run layout
npm run shared
npm run build
npm run preview
```

## Notes

- `npm run build` prepares deploy output in `dist/`.
- Edit source files in `New/` only.
- Do not manually edit `dist/`.
