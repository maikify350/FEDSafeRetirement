# Archived Database Utilities

These scripts are historical one-off utilities that were previously kept at the `WebSite/` root.

They are archived to keep the primary website workflow focused on build/deploy scripts.

## Archived Files

- `create-table.mjs`
- `get-schema.mjs`
- `list-tables.mjs`
- `test-supabase.mjs`

Use versioned SQL migrations in `supabase/migrations/` as the canonical schema history.
